"""
name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Updated to latest version

      - name: Set up Python
        uses: actions/setup-python@v4  # Updated to latest version
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Train Model
        run: |
          python ./model/train.py  # Fixed path - your files are in root, not model/

      - name: Evaluate Model
        run: |
          python ./model/evaluate.py  # Fixed path

      - name: Run Unit Tests
        run: |
          python ./tests/test_model.py  # Fixed - your test file is test_model.py, not in tests/

      - name: Upload Model Artifact
        uses: actions/upload-artifact@v4  # Updated to latest version
        with:
          name: customer-segmentation-model
          path: model/customer_segmentation.pkl

      - name: Upload Clustered Data
        uses: actions/upload-artifact@v4
        with:
          name: clustered-customer-data
          path: data/clustered_customers.csv

	-name: Deploy to render
		run: |
			curl-X POST "https://api.render.com/v1/services/${{secrets.RENDER_SERVCIES_ID}}/deploy"\
			-H"Accept: application/json"\
			-H"Authorization:Bearer${{secrets.RENDER_API_KEY}}"

"""

name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v1

    - name: Set up Python
      uses: actions/setup-python@v1
      with:
        python-version: '3.13.7'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Train Model
      run: |
        python model/train.py

    - name: Evaluate Model
      run: |
        python model/evaluate.py

    - name: Run unit tests
      run: python -m unittest discover -s tests

    -name: Deploy to Render
     run: |
        curl -X POST "https://api.render.com/v1/services/${{secrets.RENDER_SERVICE_ID}}/deploys"/
        -H "Accept: application/json"\
        -H Authorization :Bearer ${{secret.RENDER_API_KEY}}"